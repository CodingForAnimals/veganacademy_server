ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [org.codingforanimals.veganacademy.ApplicationKt.module]
  }

  jwt {
    issuer = org.codingforanimals
    secret = jwtsecret
  }

  server {
    isProd = false
  }

  database {
    jdbcDriver = org.postgresql.Driver
    jdbcDatabaseUrl = "jdbc:postgresql:veganacademy"
    dbUser = postgres
    dbPassword = 123123
  }
}